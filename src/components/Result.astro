---
interface Props {
  type: string;
  title: string;
  emoji: string;
  description: string;
  traits: string[];
  advice?: string;
  compatibility?: {
    good: string[];
    bad: string[];
  };
  lang: string;
  percentage?: number;
  totalResults?: number;
  resultIndex?: number;
}

const { type, title, emoji, description, traits, advice, compatibility, lang, percentage, totalResults, resultIndex } = Astro.props;

// Calculate display percentage (if not provided, generate a pseudo-random one based on type)
const displayPercentage = percentage ?? Math.floor(((type.charCodeAt(0) * 7 + type.length * 13) % 30) + 8);
---

<div class="result-container max-w-2xl mx-auto">
  <!-- Result Card -->
  <div class="bg-gradient-to-br from-primary-500 to-secondary-500 rounded-3xl p-1 shadow-2xl">
    <div class="bg-white dark:bg-gray-900 rounded-3xl p-8">
      <!-- Emoji & Title -->
      <div class="text-center mb-8">
        <div class="text-7xl mb-4 animate-bounce-slow">{emoji}</div>
        <div class="text-sm text-primary-600 dark:text-primary-400 font-medium mb-2">
          {lang === 'ko' ? '당신의 유형은' : 'Your type is'}
        </div>
        <h1 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white">
          {title}
        </h1>
      </div>

      <!-- Statistics -->
      <div class="mb-8 p-4 bg-gradient-to-r from-primary-50 to-secondary-50 dark:from-primary-900/20 dark:to-secondary-900/20 rounded-2xl">
        <div class="flex items-center justify-center gap-4">
          <div class="text-center">
            <div class="text-3xl font-bold text-primary-600 dark:text-primary-400">
              {displayPercentage}%
            </div>
            <div class="text-xs text-gray-600 dark:text-gray-400">
              {lang === 'ko' ? '희귀도' : 'Rarity'}
            </div>
          </div>
          <div class="w-px h-12 bg-gray-300 dark:bg-gray-600"></div>
          <div class="flex-1 max-w-xs">
            <div class="text-sm text-gray-600 dark:text-gray-400 mb-1">
              {lang === 'ko'
                ? `전체 응답자 중 ${displayPercentage}%가 이 결과를 받았어요!`
                : `${displayPercentage}% of respondents got this result!`}
            </div>
            <div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2">
              <div
                class="bg-gradient-to-r from-primary-500 to-secondary-500 h-2 rounded-full transition-all duration-1000"
                style={`width: ${displayPercentage}%`}
              ></div>
            </div>
          </div>
        </div>
      </div>

<style>
  @keyframes bounce-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  .animate-bounce-slow {
    animation: bounce-slow 2s ease-in-out infinite;
  }
</style>

      <!-- Description -->
      <p class="text-gray-700 dark:text-gray-300 text-center leading-relaxed mb-8">
        {description}
      </p>

      <!-- Traits -->
      <div class="mb-8">
        <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-4">
          {lang === 'ko' ? '주요 특성' : 'Key Traits'}
        </h3>
        <div class="flex flex-wrap gap-2">
          {traits.map((trait) => (
            <span class="px-4 py-2 bg-primary-100 dark:bg-primary-900/30 text-primary-700 dark:text-primary-300 rounded-full text-sm font-medium">
              {trait}
            </span>
          ))}
        </div>
      </div>

      <!-- Advice -->
      {advice && (
        <div class="bg-gray-50 dark:bg-gray-800 rounded-2xl p-6 mb-8">
          <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-3">
            💡 {lang === 'ko' ? '조언' : 'Advice'}
          </h3>
          <p class="text-gray-700 dark:text-gray-300">
            {advice}
          </p>
        </div>
      )}

      <!-- Compatibility (if provided) -->
      {compatibility && (
        <div class="grid md:grid-cols-2 gap-4">
          <div class="bg-green-50 dark:bg-green-900/20 rounded-xl p-4">
            <h4 class="text-green-700 dark:text-green-400 font-bold mb-2">
              {lang === 'ko' ? '잘 맞는 유형' : 'Compatible Types'}
            </h4>
            <div class="flex flex-wrap gap-2">
              {compatibility.good.map((t) => (
                <span class="text-sm text-green-600 dark:text-green-400">{t}</span>
              ))}
            </div>
          </div>
          <div class="bg-red-50 dark:bg-red-900/20 rounded-xl p-4">
            <h4 class="text-red-700 dark:text-red-400 font-bold mb-2">
              {lang === 'ko' ? '주의할 유형' : 'Challenging Types'}
            </h4>
            <div class="flex flex-wrap gap-2">
              {compatibility.bad.map((t) => (
                <span class="text-sm text-red-600 dark:text-red-400">{t}</span>
              ))}
            </div>
          </div>
        </div>
      )}
    </div>
  </div>
</div>
